gen1 <- read.csv("gen1.csv")
gen1 <- read.csv("gen1.csv",head=false)
gen1 <- read.csv("gen1.csv",head=FALSE)
t(gen1)
gen1
quit()
ls()
gen1
gen1.t <- t(gen1)
gen1.t
gen1.t[,1]
histo(gen1.t[,1])
hist(gen1.t[,1])
hist(gen1.t[,2])
hist(gen1.t[,3])
hist(gen1.t[,4])
hist(gen1.t[,5])
hist(gen1.t[,6])
hist(gen1.t[,7])
hist(gen1.t[,8])
hist(gen1.t[,9])
quit()
length(gen1.t)
summary(gen1.t)
library(ggplot2)
gen1.t
length(gen1)
gen1[,1]
gen1[1]
gen1[1,]
as.vector(gen1[1,])
gen1[1,])
gen1[1,]
gen1[,1]
gen1[,2
for (i in 1:length(gen.1)) {
    for ( j in 1:length(gen.1[,i])) {
        gen1.df <- rbind( gen1.df, data.frame(ID=j,fitness=gen1[i,j]))
    }
}
gen1.t <- t(gen1)
gen1
gen1.df <- data.frame(ID=character(),
                      Fitness=character())
length(gen1)
gen1.df <- data.frame(ID=character(),
                      Fitness=character())
for (i in 1:length(gen.1)) {
    for ( j in 1:length(gen.1[,i])) {
        gen1.df <- rbind( gen1.df, data.frame(ID=j,fitness=gen1[i,j]))
    }
}
gen.1 <- gen1
for (i in 1:length(gen.1)) {
    for ( j in 1:length(gen.1[,i])) {
        gen1.df <- rbind( gen1.df, data.frame(ID=j,fitness=gen1[i,j]))
    }
}
gen1.df
for (i in 1:length(gen.1)) {
    for ( j in 1:length(gen.1[,i])) {
        gen1.df <- rbind( gen1.df, data.frame(ID=j,fitness=gen.1[j,i]))
    }
}
gen1.df
library(ggplot2)
ggplot(vegLengths, aes(length, fill = veg)) + geom_density(alpha = 0.2)
ggplot(gen1.df, aes(fitness, fill = ID)) + geom_density(alpha = 0.2)
clear
ggplot(gen1.df, aes(fitness, fill = ID)) + geom_density(alpha = 0.2)
quit()
ls()
source('test.R')
pacman.df
source('test.R')
pacman.df
R
source('test.R')
source('kurtosis-skewness.R')
s.k
ggplot(s.k,aes(x=Skewness,y=Kurtosis,color=Gen))+geom_point()
ggsave('pacman-skewness-kurtosis.png')
quit()
y
ls()
save(pacman.df,file='pacman.Rdata')
quit()
pacman.df
write.table(pacman.df, file='pacman-fitness.csv')
quit()
